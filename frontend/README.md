# frontend

Next.js UI for AgenThink (port **3000**).

```bash
npm install
npm run dev      # development
npm run build    # production build → .next/
npm run start    # serve production build
```

API: [`../backend`](../backend) on port **8000**. Full stack: [root README](../README.md).
